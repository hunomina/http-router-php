env:
  - CODECOV_TOKEN="d01ba737-9574-47e4-b9aa-8e927c5dd1e6"

language: php

php:
  - 7.2

before_script:
  - travis_retry composer self-update
  - travis_retry composer install --no-interaction --prefer-source --dev

script:
  - php vendor/bin/phpunit --coverage-clover=coverage.xml tests

after_success:
  - bash <(curl -s https://codecov.io/bash)